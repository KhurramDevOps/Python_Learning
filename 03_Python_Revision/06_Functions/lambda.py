# LAMBDA FUNCTION 
# Its a short hand function
# A lambda function is a small anonymous function that can take any number of arguments, but can only
# have one expression.
# Syntax: lambda arguments : expression

add =  lambda x, y : x + y
print(add(2,3))

square = lambda x :  x ** 2
print(square(5))

